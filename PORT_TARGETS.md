# Port targets — `strum-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `strum` that codex-rs uses, and therefore the minimum subset that `strum-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-142] (rank `82`, tier `direct`, status `To Do`)

**Cargo package name**: `strum`
**Rust module name (in `use` statements)**: `strum`

## Summary

- **4** distinct symbols imported by codex-rs
- **10** total `use` statements
- **8** codex source files affected
- **3** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `strum::IntoEnumIterator`

- **7 `use` site(s)** across **2 codex member crate(s)**: `codex-protocol`, `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `protocol/src/openai_models.rs:12` (`codex-protocol`) — `use strum::IntoEnumIterator`
- `tui/src/bottom_pane/hooks_browser_view.rs:17` (`codex-tui`) — `use strum::IntoEnumIterator`
- `tui/src/bottom_pane/status_line_setup.rs:23` (`codex-tui`) — `use strum::IntoEnumIterator`
- `tui/src/bottom_pane/title_setup.rs:14` (`codex-tui`) — `use strum::IntoEnumIterator`
- `tui/src/chatwidget.rs:371` (`codex-tui`) — `use strum::IntoEnumIterator`
- `tui/src/chatwidget/reasoning_shortcuts.rs:19` (`codex-tui`) — `use strum::IntoEnumIterator`
- `tui/src/slash_command.rs:1` (`codex-tui`) — `use strum::IntoEnumIterator`

### `strum::AsRefStr`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `state/src/model/graph.rs:1` (`codex-state`) — `use strum::AsRefStr`

### `strum::Display`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `state/src/model/graph.rs:2` (`codex-state`) — `use strum::Display`

### `strum::EnumString`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-state`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `state/src/model/graph.rs:3` (`codex-state`) — `use strum::EnumString`
